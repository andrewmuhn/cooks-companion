<div class="row mt-4">

    {{#if recipes.length}}
    <div class="recipe-list justify-content-center" id="background-font">
        <h3>Saved Recipes:</h3>

        {{#each recipes as |recipe|}}
        <div class="row mb-2">
            <div class="col-md-6">
                <h3><a id="RLname" href="/recipes/{{recipe.id}}" onclick="event.stopPropagation();">{{recipe.name}}</a></h3>
            </div>
            <div class="col-sm-2">
                <button class="btn btn-sm btn-danger" data-id="{{recipe.id}}">DELETE</button>
            </div>
        </div>
        {{/each}}
    </div>
    {{/if}}
</div>